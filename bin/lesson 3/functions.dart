/// Functions
// 1. Объявление функций
// 2. Область видимости
// 3. Параметры, обязательные и не обязательные, именованные, значения по умолчанию.
// 4. Функции высшего порядка
// 5. Lambda функции

void main(List<String> arguments) {
  // String getFruit() {
  //   return "Apple";
  // }

  // String getFruit() {
  //   String name = 'Jack Murphy';
  //   {
  //     print(name);
  //   }

  //   return name;
  // }

  // String getFruit(String name) {
  //   return name;
  // }

  // String getFruit(String name, [int? count]) {
  //   return name;
  // }

  // String getFruit({required String name, int? count}) {
  //   return name;
  // }

  // String getFruit(String name, {int? count}) {
  //   return name;
  // }

  // String Function(int name) doubleFruit(String name) {
  //   return (int count) {
  //     return List.filled(count, name).toList().join(', ');
  //   };
  // }

  // final cb = doubleFruit('Apple');
  // print(cb(10));

  // ['Apple', 'Banana'].forEach((element) => print(element));
  // ['Apple', 'Banana'].forEach(print);
}